apply plugin: "java"
apply plugin: "application"
apply plugin: "eclipse"

repositories {
    mavenLocal()
    mavenCentral()
}

mainClassName = "Mars"

jar {
    manifest {
        baseName = "mars"
        version = "4.5"
        manifest {
            attributes "Main-Class": mainClassName
        }
    }
}
